node_modules/
.next/
.env
.env.local
.env*.local
prisma/*.db

.vercel
.env*

# Personal documents — never belong in this repo
Documents & DATA HR HELMY/

# One-off local DB migration/fix scripts — may contain real employee PII
/*.mjs
